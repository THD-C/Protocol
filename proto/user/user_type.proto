syntax = "proto3";

package user;

enum UserType {
    USER_TYPE_STANDARD_USER = 0;    // Can only display Blog 
    USER_TYPE_BLOGGER_USER = 1;     // Can edit and create Blog
    USER_TYPE_SUPER_ADMIN_USER = 2; // Can do same as Blogger and also manage users
}